cryptostream:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'postgres') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'cryptostream') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'cryptostream') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'cryptostream') }}"
      schema: public
      threads: 4
      sslmode: prefer